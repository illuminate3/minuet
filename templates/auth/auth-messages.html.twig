{% extends 'layout/base.html.twig' %}

{% block title %}{{ 'title.no_register'|trans }}{% endblock %}

{% block body %}

    <br>

    {% for flash_error in app.flashes('verify_email_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div> 3333
    {% endfor %}

    <div class="row justify-content-center mb-5">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">{{ 'title.auth_timeout'|trans }}</div>
                <div class="card-body">

                    {{ 'message.verify_timeout'|trans }}

                    <div class="form-group row mb-0">
                        <div class="col-md-12">

                            <a class="btn btn-link" href="{{ path('password_reset') }}">
                                {{ 'action.forgot_password'|trans }}
                            </a>
                            <a class="btn btn-link" href="{{ path('security_login') }}">
                                {{ 'action.sign_in'|trans }}
                            </a>


                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>

{% endblock %}
