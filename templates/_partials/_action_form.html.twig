<!-- Action -->
<div class="row mt-4">
    <div class="col">


<div class="form-group">

    <button type="submit" class="btn btn-success">
        {{ 'action.save'|trans }}
    </button>

    <a class="btn btn-danger" href="{{ path(cancel_url) }}">
        {{ 'action.cancel'|trans }}
    </a>

</div>

    </div>
</div>
