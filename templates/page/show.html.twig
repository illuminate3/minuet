{% extends 'layout/base.html.twig' %}

{% block subheader %}
{% endblock %}

{% block body %}

    <div class="row">
        <div class="col">

            <h1>
                {{ page.title }}
            </h1>

            <p class="lead">
                {{ page.description }}
            </p>

        </div>
    </div>

    <div class="row">
        <div class="col">

            {{ page.content | raw }}

        </div>
    </div>

{% endblock %}
